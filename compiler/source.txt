asm{
	entry code
	;data@ data
code:
    call arrDeclare
	hlt
};

fun print(var value)
{
	// store r0, pull the argument into a register, then output that register
	asm{
		push %r0
		mov %r0, 4(%bp)
		out %r0
		pop %r0
	};
}

fun arrDeclare()
{
    var *array = 512;
    array[0] = 1234;
    
    print(array[0]);
    array = 0;
}

$$