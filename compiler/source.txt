
# this function will (one day) compute all fibonacci numbers <= n 
fun fibb(var n){
    var z = fibb(n - 1) + fibb(n - 2);
    
    
    #var a = n + 1;
    #var b = (a + 1) - (a + (18 - a));
    
    #var z = 1 + fibb(n - 2);
}

fun double(var x)
{
    x = x + x;
}


fun main(var a){
    a = fibb(a);
}
$$