
# this function computes the nth fibonacci number 
fun fib(var n)
{
    if(n < 2)
        return n;
    else
        return fib(n - 1) + fib(n - 2);
    
}

fun firstnfibs(var n){
    var i = 0;
    fib(i);

}



#fun main(var a)
#{
#    a = fibb(a);
#}
$$