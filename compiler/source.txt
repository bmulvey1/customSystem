
# this function will (one day) compute all fibonacci numbers <= n 
fun fibb(var n)
{
    var answer;
    if(n <= 1){
        answer = n;
    }else{
        answer = fibb(n - 1) + fibb(n - 2);
    }
    return answer;
}

#fun main(var a)
#{
#    a = fibb(a);
#}
$$