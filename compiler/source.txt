asm
{
    #include "CPU.asm"
    entry code
code:
    push $512
    call test
code_done:
    hlt
};

fun fib(var n){
    if(n < 2){
        return n;
    }else{
        return fib(n - 2) + fib(n - 1);
    }
}


$$
